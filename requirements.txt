# --- Streamlit and Logic ---

streamlit
langchain-core
langchain-community
langchain-text-splitters
langchain-huggingface
pydantic-settings

# --- Machine Learning (CPU-only for Cloud) ---

We use standard names without complex flags to avoid 'uv' parsing errors

torch
sentence-transformers
chromadb

# --- LlamaCpp ---

Note: Building this on Streamlit Cloud frequently fails due to 1GB RAM limits.

llama-cpp-python

# --- Backend ---

firebase-admin
google-cloud-firestore
